ssh sandman -l sandcastle "echo $PATH" 
does nozt have the docker path!

---

[Request interrupted by user for tool use]

---

so the installer needs to write to ~/.profile - do not touch /etc/profile