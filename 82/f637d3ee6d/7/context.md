# Session Context

## User Prompts

### Prompt 1

lets setup the traefik routes for vnc and ttyd when starting the castle so it's always there and we never have to wait.

### Prompt 2

can we remove the wait pages for ttyd an vnc now?

### Prompt 3

commit this

### Prompt 4

lets look at https://github.com/thieso2/Sandcastle/issues/7

update the plan to match the lastest state of the app.

### Prompt 5

let's build the web ui for adding/removing routes

### Prompt 6

explain whet the routs do.

### Prompt 7

can traffic do ssh multi-home or only https?

### Prompt 8

explain SNI-based TCP routing — works only if SSH is wrapped in TLS first (e.g. ssh over a TLS tunnel). Standard ssh clients don't do this.

### Prompt 9

okay. so i want HTTPS forwarding with SNI and add a mode for a "normal" port forward that would create a static port on the public traaefik side.

### Prompt 10

[Request interrupted by user for tool use]

