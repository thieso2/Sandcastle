craeet GH issute to add sudo inside to the sandcaste installer, admin and control scriots when sudo is needed so that i nerver have to use "sudo command"

---

if we have smb availble make hoem and dta (rename to persisted) cliakble to do the smb mount. change teh look of the labesl so that it's obvouus that it can be clicked.