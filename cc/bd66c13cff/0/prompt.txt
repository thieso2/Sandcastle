wil a new sandcastel install still use 220568 as UID/GID?

---

fiox teh merge conflin in the PR

---

run the tests!

---

2

---

merge PR#86 and release a new version!

---

where can i cretae a Project?

---

when using a project - add the project by to the container name project:name that allows one "temp" container per project.

---

i hate if entrypoint does a chown -R as it can be slow!

---

what went wrong when the image was generated - do we not know what UID will be used when the container is launched? (1000)

---

yes

---

commit push and release new version

---

teh sandcastle name shoudl contain teh project name : project:sandcaste

---

when starting a terminal in a sandcaste cwd shoule be $HOME not /

---

PATH does not work!
thies@test-p3:~$ claudebash: command not found: claudethies@test-p3:~$ echo $PATH/usr/local/share/mise/installs/node/lts/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/binthies@test-p3:~$

fix and test!

---

add a project CRUD in the user settings! also add a project filter to the dashboard WEB-UI.