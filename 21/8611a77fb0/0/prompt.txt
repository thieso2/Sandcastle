add SANDCASTLE_ADMIN_USER (name of the user to create) to installer.sh

---

commit and push

---

change the postgres data dir from $SANDCASTLE_HOME/pgdata to $SANDCASTLE_HOME/data/postgres in all code and scripts