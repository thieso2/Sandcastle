# Session Context

## User Prompts

### Prompt 1

we have added "Persisted Directories" and "Injected Files". this may be overkill?

what we wanted to produce is that i can update claude in a sandbox. this may be overkill - all thaths needed is to install claude outside of $HOME for the base-image and copy it into the base image (when home is mounted) and skip the copy if claud is already there (updated by the user).
exploare and propose a smart solution.

### Prompt 2

yes

### Prompt 3

commit and push

