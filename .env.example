# Rails secret key (generate with: bin/rails secret)
SECRET_KEY_BASE=

# Public hostname of the Sandcastle server
SANDCASTLE_HOST=sandcastle.example.com

# Data directory for persistent volumes (default: /data)
# SANDCASTLE_DATA_DIR=/data
